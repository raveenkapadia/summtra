Found 12 bugs in the International Wealth PDF. Here's the prioritized fix list:

## 🔴 CRITICAL (Fix First)

### BUG 1: Hindi Text Truncated
Shows: चं, गु, शु, लन, रा, न, वृवृक
Should: चंद्र, गुरु, शुक्र, लग्न, राशि, नक्षत्र, वृश्चिक

FIX: Check these template files for text overflow:
- planetary-lines-page.html → .planet-hindi class
- glossary-page.html → .glossary-hindi class  
- vedic-profile-page.html → .vedic-card-hindi class

Either increase container width, reduce font-size, or remove overflow:hidden.

### BUG 2: Planetary Lines Page Duplicated 4x
The same page appears 4 times in a row.

FIX: Check pdfAssembler.js - find where planetary-lines-page is added and ensure it's only added ONCE.

### BUG 3: Cities to Avoid Page - 5x Duplicate + NO CITIES LISTED
Page appears 5 times but shows NO actual cities to avoid.

FIX: 
1. Find where avoid cities are passed to template - {{AVOID_CITIES_CARDS}} is empty
2. Ensure the 5 lowest-scoring cities are populated
3. Page should only appear ONCE

### BUG 4: City Rankings Missing Cities 6-9
Page 1 shows ranks 1-5, Page 2 jumps to 10-12.

FIX: Check city-ranking-table.html pagination logic. Should show:
- Page 1: Cities 1-5
- Page 2: Cities 6-10
- Page 3: Cities 11-12 (if needed)

### BUG 5: Power Zones Shows Coordinates
Shows: "Favorable at 23.02°, 72.57°" (birth coordinates!)
Should: A count like "3" or actual power zone names

FIX: Check {{POWER_ZONES}} variable in templateProcessor.js - it's outputting lat/lng instead of zone count.

### BUG 6: Goal Icon Missing
{{GOAL_ICON}} not rendering on goal-divider-page.

FIX: Check if GOAL_ICON variable is being set. Should be 💰 for Wealth.

## 🟠 HIGH PRIORITY

### BUG 7: "Q" Renders as "u"
"Quick Reference" shows as "uick Reference"
"Qatar" shows as "atar"

FIX: Font issue with Cormorant Garamond or Montserrat. Check if fonts are loading correctly in Puppeteer.

### BUG 8: Regional Maps Empty
All map pages (World Overview, Europe, Tokyo Region, etc.) have no SVG content.

FIX: Check map-renderer.js - SVG is not being generated or inserted into {{MAP_SVG}}.

### BUG 9: Empty Rows in Ranking Table
Page 2 shows "— %" placeholder rows.

FIX: Template should not render empty city slots. Add conditional: only render if city exists.

### BUG 10: Timing Symbol Not Rendering
"⧗" shows as empty box.

FIX: Replace with "⌛" or use text "TIMING" instead.

---

After fixing, please:
1. Run all tests (should still be 285+ passing)
2. Regenerate PDF for Raveen Kapadia (International, Wealth)
3. Share new PDF link for verification